/**
 * Release gate for Agents Center navigation.
 *
 * The center projects the existing AgentRun hierarchy across Chat, Work, and
 * Code. It reuses the same control commands as thread-level hierarchy panels
 * and does not introduce a second execution model.
 */
export const AGENTS_CENTER_NAVIGATION_ENABLED = true;
