/**
 * Faint Distilled “code rain” lines for the mobile canvas.
 * Owned sample snippets — not third-party product copy.
 */
export const CODE_PATTERN_LINES = [
  "const host = await pair({ origin, ticket })",
  'thread.mode === "chat" ? steer() : inventory()',
  "if (!staleGate.allow) return failClosed()",
  "journal.append(event) // projections rebuild",
  "provider.capabilities.reportHonestly()",
  "sandbox.confine(projectRoot)",
  'approval === "gated" ? defer() : execute()',
  "remote.client.authority <= host.authority",
  'export type Mode = "chat" | "work" | "code"',
  "Effect.gen(function* () { yield* resource })",
  'secureStore.set("deviceKey", keyId)',
  "board.done iff deliveryTarget.satisfied",
  'skills.discover(".agents/skills")',
  "transport.reconnect({ replayFrom })",
  "policy.failClosed(unsupported)",
  'canvas.material = "liquid-glass"',
] as const;
